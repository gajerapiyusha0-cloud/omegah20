# Observability

- Prometheus text: `GET /metrics`
- Health: `GET /api/v1/health`
- Admin flags: `GET /api/v1/admin/console`

Scrape `geotwin_up`, `geotwin_domains_total`, `geotwin_simulations_total`, `geotwin_db_up`.
